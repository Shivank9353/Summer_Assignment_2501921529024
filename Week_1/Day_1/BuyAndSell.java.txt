class BuyAndSell {
    public int maxProfit(int[] prices) {
        if (prices == null || prices.length == 0) {
            return 0;
        }

        int minPrice = prices[0];
        int maxEarned = 0;

        for (int i = 1; i < prices.length; i++) {
            minPrice = Math.min(minPrice, prices[i]);
            maxEarned = Math.max(maxEarned, prices[i] - minPrice);
        }
        return maxEarned;
    }
}